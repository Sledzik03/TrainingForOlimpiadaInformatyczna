#include <bits/stdc++.h>
using namespace std;

int n;
int dl[11000000];
int nwd();
int main()
{
    cin >> n;
    for (int i=0;i<n;i++)
    {
        cin >> dl[i];
    }
    for (int i=0;i<n;i++)
        sort(dl,dl+n);
    while()
}
